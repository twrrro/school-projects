<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:padding="12dp"
    android:gravity="center_vertical">

    <TextView
        android:id="@+id/textViewFavoriteJokeText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:text="Fıkra metni buraya gelecek"
        android:textAppearance="?attr/textAppearanceBody1"
        android:layout_marginEnd="8dp"/>

    <ImageButton
        android:id="@+id/buttonUnfavoriteJoke"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_favorite_filled"
        app:tint="?attr/colorError"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Favorilerden Çıkar"
        android:minHeight="48dp"
        android:minWidth="48dp"
        android:padding="8dp"/>

    <ImageButton
        android:id="@+id/buttonShareFavoriteJoke"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_share"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="Favori Fıkrayı Paylaş"
        android:minHeight="48dp"
        android:minWidth="48dp"
        android:padding="8dp"/>

</LinearLayout>
